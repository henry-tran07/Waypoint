// Copy this file to `Mira/Secrets.swift` (which is gitignored) and fill in real keys.
// Both keys are compiled into the app binary — fine for hackathon demo, NOT for App Store distribution.
//
// xcodegen reads sources from `Mira/` matching `*.swift`, so this `.example` file is NOT compiled.
//
//     import Foundation
//
//     // Anthropic — used by DetectionService for Claude Vision frame analysis.
//     let anthropicAPIKey = "sk-ant-…"
//
//     // Google Gemini — used by GeminiLiveService for the conversational voice loop.
//     // Get one at https://aistudio.google.com/app/apikey  (the same API key works for Gemini Live).
//     let geminiAPIKey = "AIza…"
